"""
==========================================================================
 prompts.py —— 流水线全部节点 Prompt 集中管理文件
==========================================================================

设计契约（执行方案 §5.3 人格三层 + §4 各节点职责 + §3 deepagents 架构）：

- 每个 builder 返回 (system, user) 二元组，交给该节点的 deepagents 实例执行；
- system = 节点标记 + 人格 L1 常驻前缀（persona-writer 全文，100% 在场）
         + 已挂载技能清单（deepagents 渐进加载：清单引导 agent 用 read_file
           读取 /skills/<name>/SKILL.md 手册正文，不再全文塞进 prompt）
         + 人格 L2 系列记忆；
- user   = 节点输入契约（PipelineState 字段子集）+ 输出 Schema
         + 打回意见段（重跑时注入，「按条目修改，不重写全文」）；
- system 第一行固定为 <!-- NODE:节点id --> 标记：
  ① MockLLM 据此路由（测试/演示）② 节点日志可审计；
- 各节点默认挂载技能 = 执行方案 §5.1 挂载表（app/agents/node_registry.py），
  前端改挂载后由节点函数把实际清单传进来（mounted_skills 参数），
  prompt 清单与实际挂载永远一致。

节点索引（命名即含义）：
  N1 build_n1_event_card_mining      史料分析 · 事件卡选矿   → historical-event-cards
  N2 build_n2_style_robe_selection   弹药挂载 · 风格军械师   → style-library
  N3 build_n3_outline_blueprinting   大纲生成 · 蓝图绘制     → outline-architect
  N4 build_n4_chapter_construction   旁白写作 · 逐章施工     → narration-writer
     build_n4_full_script_stitch     旁白写作 · 全稿缝合     → narration-writer
  N5 build_n5_three_gate_audit       成稿审核 · 三道门禁
  N6 build_n6_storyboard_translation 画本加工 · 声音翻译

技能接口链（各节点 user 提示词已对齐 skill 契约，2026-09 修订）：
  N1 产出六型资料卡包（C/P/R/B/D/E，validate_cards.py 过闸）
    → N3 消费资料卡包，按大纲方法论 v3 产出《大纲包》＝篇级总卡＋段级施工卡
       （outline-architect，check_outline.py 过闸）
    → N2 消费《大纲包》做全篇弹药挂载（style-library：查库/自造/联网三通道
       取弹，密度红线体检），输出挂载后大纲＋弹药挂载报告
    → N4 消费挂载后大纲 + 资料卡包，逐章施工后缝合
       （check_output.py 过闸）
==========================================================================
"""
from __future__ import annotations

import json

from . import memory_store
from .agents.node_registry import DEFAULT_SKILL_MOUNTS
from .skills_loader import load_skill, skill_description

# ---------------------------------------------------------------- 公共件

def _j(obj) -> str:
    return json.dumps(obj, ensure_ascii=False, indent=2)


def persona_prefix() -> str:
    """人格 L1 常驻前缀：persona-writer 全文，每个内容节点 100% 在场。"""
    return load_skill("persona-writer")


def series_memory_section(node_uses: list[str]) -> tuple[str, list[str]]:
    """人格 L2 系列记忆：返回 (prompt段文本, 实际加载的记忆文件清单)。"""
    parts, loaded = [], []
    if "lessons" in node_uses:
        txt = memory_store.read_memory(memory_store.LESSONS)
        if txt.strip():
            parts.append(f"## 系列记忆 · 打回教训沉淀（历次人工打回的教训，务必避开）\n{txt}")
        loaded.append(memory_store.LESSONS)
    if "voice_samples" in node_uses:
        txt = memory_store.read_memory(memory_store.VOICE_SAMPLES)
        if txt.strip():
            parts.append(f"## 系列记忆 · 声口样句库（历期通过的语气示例，声口向它对齐）\n{txt}")
        loaded.append(memory_store.VOICE_SAMPLES)
    return ("\n\n".join(parts), loaded)


def rework_section(feedback: str | None, prev_artifact_brief: str | None) -> str:
    """打回意见注入段（执行方案 §6.3 上下文纪律）。"""
    if not feedback:
        return ""
    return (
        "\n\n## ⚠️ 打回重跑指令（最高优先级）\n"
        "上一版产物被人工闸门打回。纪律：**按打回条目逐条修改，不顺手重写全文"
        "——重写是偷懒，修改才是施工。**\n\n"
        f"【打回意见】\n{feedback}\n\n"
        f"【上一版产物（在此之上修改）】\n{prev_artifact_brief or '（见上方输入）'}\n"
    )


def _mounted_skills_section(mounted_skills: list[str]) -> str:
    """已挂载技能清单：引导 deep agent 走渐进加载（read_file 读手册正文）。

    技能全文不再塞进 system——这是 deepagents skills 机制的核心收益：
    清单只占几行，正文由 agent 按需读取，且前端改挂载后此处自动同步。
    """
    lines = ["# 本节点挂载技能（手册在虚拟文件系统 /skills/ 下，渐进加载）"]
    for i, name in enumerate(mounted_skills):
        tag = "（本节点主技能）" if i == 0 else ""
        lines.append(f"- **{name}**{tag}：{skill_description(name)}")
    lines += [
        "",
        "⚠️ 开工纪律：动手前先用 read_file 通读主技能手册 /skills/<主技能名>/SKILL.md 全文"
        "（其余技能按任务需要阅读），严格遵循其中的 SOP、Schema 与禁忌清单。",
        "⚠️ 输出纪律：最终结果必须直接写进你的最终答复——流水线只从最终答复提取 JSON，"
        "不要只把结果写进文件。",
    ]
    return "\n".join(lines)


def _system(node_marker: str, mounted_skills: list[str],
            memory_uses: list[str]) -> tuple[str, list[str]]:
    parts = [
        f"<!-- NODE:{node_marker} -->",
        "# 人格层（L1 常驻，你的操作系统）\n" + persona_prefix(),
    ]
    if mounted_skills:
        parts.append(_mounted_skills_section(mounted_skills))
    mem_txt, loaded = series_memory_section(memory_uses)
    if mem_txt:
        parts.append(mem_txt)
    return "\n\n---\n\n".join(parts), loaded


# ---------------------------------------------------------------- N1 史料分析

def build_n1_event_card_mining(state, feedback=None, prev=None,
                               mounted_skills=None) -> tuple[str, str, list[str]]:
    system, loaded = _system(
        "n1_event_card_mining",
        mounted_skills or DEFAULT_SKILL_MOUNTS["n1_event_card_mining"],
        memory_uses=["lessons"],
    )
    user = f"""## 输入：任务信息
- 史料类型：{state["source_type"]}（dynasty 朝代 / person 人物 / event 事件）
- 目标时长：{state["target_minutes"]} 分钟
- 集次：第 {state["episode_no"]} 集
{f"- 上集衔接段：{state.get('prev_episode_bridge')}" if state.get("prev_episode_bridge") else ""}

## 输入：史料原文
{state["source_text"]}

## 任务：按主技能（historical-event-cards）「六步取证流程」执行
1. **读料判型**：通读史料，确认题材类型、覆盖时间段与出场人物；
2. **拆主干事件卡 C-**：一卡一事，装不下就拆（"鸿门宴"必须拆成赴宴/舞剑/闯帐/尿遁四张）；同一事件多个史料版本分别成卡、备注互标卡号，不抹平分歧；先标可信度初判。事件卡 ≥5 张；
3. **缺口诊断**：对照下游需求盘点——人物只有名字没有性格→补人物卡 P-；博弈关系说不清→补关系卡 R-；制度/地理门槛高→补背景卡 B-；宏大判断没数字托底→补数据卡 D-；有歇后语/相似事件/冷知识/名言→补彩蛋卡 E-（有就拆，没有不强凑）；
4. **定向搜索补料**：每个缺口按手册 references/research-playbook.md 的查询公式联网搜索（环境无搜索工具时凭已有史料拆卡，缺口在备注标"需补料"）；来源优先级：正史原文＞学术机构/博物馆/考古报告＞权威媒体＞百科（仅作线索不作依据）；搜不到标"存疑"，严禁脑补填充；
5. **多源互证与定级**：关键事实（时间/数字/死因/胜负）力争 ≥2 个独立来源，孤证标"存疑"；终审定级 正史/诸子/传说/出土文献 四级，传说级限位（仅钩子/彩蛋/氛围可用）；
6. **写卡校验**：按手册 references/schema.md 的字段规格填卡，然后过出口闸门。

## 字段要点（细则以 schema.md 为准）
- 卡号：卡型前缀 + 三位数字各自递增（C-001、P-001……），全库唯一；
- 每卡必有：对应卡型必填字段 + 史料出处（具体书名/篇目，多源用 `/` 分隔，网搜补料写明具体来源，不许只写"网络"）+ 可信度；
- 事件卡加标：叙事角色（钩子/锚定/主线节点/插件/高潮/收束/衔接，可复选）+ 本集用不用（用/备用/不用）——下游按这两个字段筛选与布点；
{f'- 人物篇加标：人生阶段、弧光位置、颠覆印象度（高/中/低）。' if state["source_type"] == "person" else ""}
{f'- 事件篇加标：因果位置（远因/近因/导火索/结果/余波）、事件阶段、多方视角。' if state["source_type"] == "event" else ""}
{f'- 朝代篇加标：因果位置、制度名称，并留意时代氛围判断所需的背景卡。' if state["source_type"] == "dynasty" else ""}
- **只记事实，不预判弹药**：禁止"弹药潜质/亮点类型"类风格字段（弹药由大纲节点从风格层弹药库统一选配，本层越权预判会污染下游）；有可逐字引用的原文照录进「古文原句」并标出处——原文是史料，怎么炸是下游的事；
- 可选字段有内容才写，没有整个省略，不写空字符串凑格式。

## 出口硬闸门（必过）
卡包写入文件后运行校验脚本，FAIL 项修完复检，全绿（或仅剩能说明理由的 WARN）才可交付：

python3 /skills/historical-event-cards/scripts/validate_cards.py <卡包.json>

## 输出
只输出一个 JSON 数组（不要任何解释文字），元素为六型资料卡对象（C/P/R/B/D/E 混排，卡号即身份）。
"""
    user += rework_section(feedback, prev)
    return system, user, loaded


# ---------------------------------------------------------------- N2 弹药挂载

def build_n2_style_robe_selection(state, feedback=None, prev=None,
                                  mounted_skills=None) -> tuple[str, str, list[str]]:
    system, loaded = _system(
        "n2_style_robe_selection",
        mounted_skills or DEFAULT_SKILL_MOUNTS["n2_style_robe_selection"],
        memory_uses=["lessons", "voice_samples"],
    )
    used_cards = [c for c in state.get("event_cards", []) if c.get("本集用不用") != "不用"]
    user = f"""## 输入：大纲包（被挂载对象——段级施工卡 N 张，核心消费字段：段意/功能类型/使用史料/指定技法/弹药清单/歇后语清单/情绪坐标/备注）
{_j(state.get("outline", []))}

## 输入：资料卡包（史实锚点参考——「先支撑后弹药」：每发弹药先找史实逻辑支撑点，再选弹药词，支撑点挂卡号）
{_j(used_cards)}

## 输入：任务参数
- 类型：{state["source_type"]}（dynasty 朝代 / person 人物 / event 事件）
- 目标时长：{state["target_minutes"]} 分钟｜第 {state["episode_no"]} 集

## 任务：按主技能（style-library）做全篇风格化弹药挂载——「给大纲装弹」，不重排结构、不写一句正文
0. **读懂大纲**：主题/类型/朝代＋每段一句话内容，找出核心爽点；逐段扫描语义机会（人物纠结→心理机会、权谋操作→动作机会、下论断→定性机会、预期违背→反讽机会、需权威托底→史料机会），无机会处显式标注「不挂」；
1. **洞察爆炸点**：以敏锐编辑之眼扫六类爆炸点（数字反差/身份倒错/伦理冲击/时代错位/命运反转/留白脑补）——「观众会截图哪一句？」至少重装 1 个；一个都锁不住则提示「大纲可能缺爆点」；
2. **三通道取弹**：A **查库**（优先；按需读 /references/ 下对应弹药类型，不要一次全读，先明确要什么再定位）→ B **自造**（库存没有时按 generation-rules.md 现场生成，标【自造】＋史实锚点）→ C **联网热梗**（需当期热点时按 meme-scouting.md 检索→筛选→降维改造，标检索时效）；
3. **挂载原位**：只增量填充「指定技法／弹药清单／歇后语清单／备注（仍只写施工指令，禁写正文句子）」，**不动段意、不重排结构、不改史料清单与伏笔台账**；弹药条目格式「弹药名（类型）｜作用｜推荐度（强烈推荐≤4处/可选）｜原因」，附用法提示；爆炸点与核心爽点位配最强弹药，修辞弹药（成语/歇后语/化用）挂刀刃位；
4. **体检两道线**：位置底线（开篇钩子/高潮反讽/史料托底/收束金句四处充要特征；底线处缺料标「补位建议」退回，不硬凑）＋ 密度红线（黑话 ≥6 建议 8–15、代入 ≥3、史料 ≥2、反讽 ≥2、同词 ≤2）；
5. **同步篇级**：弹药总表查重（同词 ≤2 次、同类不连续两段）结果写进挂载报告。

## 边界纪律（违反即打回）
- **增量不越权**：装配器不是编辑器——结构问题退回大纲节点，只标注不擅自处理；
- **弹药跟内容走，不跟位置走**：没有对应内容就不挂，撑不起的位置宁可空着（空胜过硬凑）；
- **悲悯段禁区**：弹药/绰号/设问全清零，只许 [史料引用]，禁挂任何娱乐性弹药；
- **杜撰史料引文即违规**：修辞弹药可制可造，史料引文必须真实可考，库中没有就标注缺口；
- **验梗时效**：用过气梗或气质不合的梗，尬比缺梗更伤。

## 输出 Schema（只输出一个 JSON 对象）
{{
  "挂载后大纲": [
    {{ "施工卡号": "S-001",
       "指定技法": ["..."],
       "弹药清单": "弹药名（类型）｜作用｜推荐度｜原因＋用法提示",
       "歇后语清单": "歇后语＋挂接事件＋建议入点＋（传统/自创）；无则 空",
       "备注": "增量补充的施工指令（原备注内容保留），禁写正文句子",
       "其余原卡字段": "原样保留、不得改动" }}
  ],
  "弹药挂载报告": {{
    "挂载点台账": [{{ "段号": "S-001", "风格类型": "...", "触发信号": "...", "史实支撑点": "C-001", "弹药（来源：库取/自造/联网）": "...", "推荐度": "强烈推荐|可选" }}],
    "弹药台账": ["每发弹药：来源｜挂载位置｜史实锚点｜挂载理由"],
    "未挂载说明": ["识别出但放弃挂载的位置及原因（撑不起/密度满/悲悯禁区）"]
  }}
}}
"""
    user += rework_section(feedback, prev)
    return system, user, loaded


# ---------------------------------------------------------------- N3 大纲生成

def build_n3_outline_blueprinting(state, feedback=None, prev=None,
                                  mounted_skills=None) -> tuple[str, str, list[str]]:
    system, loaded = _system(
        "n3_outline_blueprinting",
        mounted_skills or DEFAULT_SKILL_MOUNTS["n3_outline_blueprinting"],
        memory_uses=["lessons"],
    )
    used_cards = [c for c in state.get("event_cards", []) if c.get("本集用不用") != "不用"]
    user = f"""## 输入：素材卡包（唯一事实来源，N1 已过闸；六型卡 C/P/R/B/D/E，卡上没有的史实一个字不许进大纲）
{_j(used_cards)}

## 输入：任务参数
- 类型：{state["source_type"]}（dynasty 朝代 / person 人物 / event 事件）｜目标时长：{state["target_minutes"]} 分钟｜第 {state["episode_no"]} 集
{f"- 上集衔接段（本篇开头要接住）：{state.get('prev_episode_bridge')}" if state.get("prev_episode_bridge") else ""}

## 任务：按主技能（outline-architect）「历史长视频文案大纲方法论 v3」执行——本技能只产大纲，不写正文
- **输入判读**：先清点卡片再动笔——C 卡够不够撑主线、P/R 卡人物与张力、B 卡结构归因弹药、D 卡数字托底、E 卡仅限氛围禁入悲悯段；人物动机若无心理史料只能记【I｜推断】；
- **第〇步 选题价值诊断**（三问全答才进入第一步）：①观众原本相信什么 ②看完之后不得不重新相信什么（认知落差，必须一句话说清）③删掉这个题观众损失什么——答案只是「了解了一段历史」则退回换题或换主角；悖论分级：事实反差只够短视频，**因果反转／价值反转才合格，本节点只受理后两类**；
- **第一步 驱动分型＋结构选型**：记住人/规律→单点篇（问题驱动）；记住进程→编年篇（主线悬念驱动）；结构选型 A人物认知／B事件翻案／C进程悬念／D制度解释／E群体命运——**五段流水线是唯一骨架**，题材类型只决定段落里装什么、按什么顺序装；变体配置（如无翻案）须在总卡明示是变体而非漏写；
- **第二步 选题与标题**：反常识对象＋归因式追问（为什么/怎么会）＋语气承诺三件套缺一不可；1 主推＋2 备选，可加双问句副标；
- **第三步 一句话论点＋主线封存＋认知变化链（人物篇加弧光）**：论点压缩为一句可证伪判断（判断素材挂不挂主线的唯一标尺）；封存素材两级判据——①挂不上主线 ②挂得上但只为好看不推进论点（「精彩但删除」），标去向（赘肉/未来篇目）；认知变化链六环（原信→动摇→失效→重建→新认知→留白），**内容从卡片推导、每环挂卡号**，换素材换链，推导不出说明诊断没做透；人物篇填弧光七步（初始→第一次选择→反馈→修补→再选→路窄→最终选择→重新认识），每步挂卡号、无卡支撑标「缺料，需补查」，禁止虚构；
- **第四步 五段流水线布点**：开场 5–10% 只呈现「不对劲」｜翻案 0–25% 把旧结论辩护到最高点（素材无案可翻时用无翻案变体，翻案与反转先后两步不可互替）｜证据群 35–60%（3–5 分论点，三新规：每个分论点写「认知移动：从___到___」写不出就整组砍掉／证据按因果递进排列不许并列堆放／每个分论点列【最强反证】＋【反证处理】，找不到标「待补查」）｜理性人反转 15–20%（三规：透镜唯一且必须从卡片长出——史料＞因果＞理论／重大因果填跨度四字段【原因·时间｜中间变量｜结果·时间｜强度】／收束前落「理解不等于原谅，解释不等于免责」）｜收束 5–10% 无「综上所述」、末句可原封充当下篇开场；**段间无过渡段**，上一段最后一个元素即下一段第一个攻击目标；
- **第五步 布点四件事**：悲悯段落清单（标悲悯轨，写作时不得临时改轨；弹药/绰号/设问全清零，进入后不回跳愤怒/讽刺轨）｜签名句式落点（同类不连用、设全篇总上限、只服务节奏不承担事实）｜缓冲/商单坑位（证据群中点后、情绪过载拐点前；有商单埋语义钩连句）｜互动钩子 ≥1 处（证据群阵间或收束段前）；另锁定一个可详细描述的单段趣事；
- **可信度台账 F/I/T**：F 史料直引（年号＋公元双标）／I 推断标【I｜推断类型｜依据卡号】／T 理论标【T｜透镜名｜指向卡号】；铁律 **I 不得写成 F、T 不得写成 F**，孤证一律降 I 标「存疑」，宏大判断必须有 D 卡数字托底；台账逐条列出全篇所有 I 与 T，附在篇级总卡之后；
- **交付前逐条过主技能质检清单**（含机检项：S-编号连续、伏笔有埋有收、弹药查重、悲悯段清零、字数 ±10%、因果接口无空转、相邻炸点不同型、透镜唯一、收束无「综上所述」、签名句式不连用、缓冲坑位位置、互动钩子 ≥1、F/I/T 无冒充），全绿才交付。

## 卡面纪律（字段规格以 references/card-spec.md 与 assets/outline-template-v3.md 为准）
- **生成顺序**：先逐段填段级施工卡（S-001 起连续递增），篇级总卡各表（情绪地图/炸点总表/伏笔台账/弹药总表等）由施工卡对应字段**聚合生成**，不在总卡凭空填写；
- **篇级总卡一张**：标题（主推＋备选＋副标）／内置悖论（含第〇步分级结论）／选题诊断三问／认知落差／驱动分型＋结构选型（含变体配置）／一句话论点／认知变化链／人物弧光（仅人物篇）／主线因果链（重大因果附跨度检查）／目标时长→字数预算（×270 字/分钟；未指定默认 25 分钟≈6750 字）／篇幅配比／理论透镜（唯一＋从哪些卡片长出）／语气基调／开头前置／结尾呼应／声音素材（绰号候选＋签名句式落点）／伏笔总台账／炸点总表／情绪地图／布点四件事／弹药总表（查重）／字数预算表／自补清单／封存素材／F-I-T 可信度台账／接缝（单篇填「无」）；
- **段级施工卡基础字段**：五段归属＋模块标签（钩子/插播/缓冲-商单/定场/透镜/视角切换/互动钩子）／段意（写不出＝这段不该存在）／功能类型／时间码（累计字数 ÷ 270 字/分钟）／使用史料卡／节点分级／指定技法（悲悯段只许 [史料引用]）／因果接口（「本段之因 ← S-___ ｜ 本段之果 → S-___」，填不出因的段当场修掉）／弹药清单（精确到卡号，强烈推荐 ≤4 处）／史料清单（古文原句照录不动）／歇后语清单（空胜过硬凑）／相似事件／炸点／伏笔／情绪坐标（悲悯显式标出）／预计字数／备注（只写施工指令，禁写正文句子）；
- **按五段归属追加规程字段**：开场卡（反常细节选点/定格瞬间/开场之问）｜证据群卡（分论点/阵型/认知移动/最强反证＋反证处理/定音句）｜反转卡（冻结声明/选项集/透镜/因果跨度四字段/价值兜底落点）｜收束卡（形态/定格单位/甩钩/留白＝认知变化链最后一环）——按归属只填对应一组。

## 出口硬闸门（必过）
大纲与事件卡分别写入文件后运行检查脚本，FAIL 项修完复检，全绿才可交付：

python3 /skills/outline-architect/scripts/check_outline.py <大纲.md> --events <事件卡.txt> --minutes {state["target_minutes"]}

## 输出 Schema（只输出一个 JSON 对象——卡面字段与模板一一对应，流水线只从最终答复提取 JSON）
{{
  "篇级总卡": {{
    "题目集名": "主推标题；备选①…②…；副标（双问句，可无）",
    "内置悖论": "表层常识 vs 深层事实；分级结论：因果反转／价值反转",
    "选题诊断": "原信什么…｜不得不改信什么…｜删掉损失什么…",
    "认知落差": "一句话说清本篇核心认知冲突",
    "驱动分型": "单点篇／编年篇＋判定理由（想让人记住人/规律/进程）",
    "结构选型": "A人物认知／B事件翻案／C进程悬念／D制度解释／E群体命运＋变体配置说明",
    "一句话论点": "一句可证伪的判断",
    "认知变化链": "原信{{卡号}}→动摇{{…}}→失效{{…}}→重建{{…}}→新认知{{…}}→留白{{…}}",
    "人物弧光": "仅人物篇：初始→第一次选择→现实反馈→修补→第二次选择→环境收紧→最终选择→重新认识；每步挂卡号，缺料标「缺料，需补查」",
    "主线因果链": "因为……所以……最终……（重大因果附：中间变量｜强度 强/中/弱）",
    "目标时长": "{state["target_minutes"]}分钟",
    "总字数预算": 0,
    "篇幅配比": "开场__%｜翻案__%（无翻案填0并标变体）｜证据群__%｜反转__%｜收束__%",
    "理论透镜": "全篇唯一；从哪些卡片长出：C/B 卡号",
    "语气基调": "戏谑/悲悯/热血 组合",
    "开头前置": "冷开场：反常细节选帧＋理由／热开场：上期收束点回收",
    "结尾呼应": "收束形态＋开头设问回收方式＋定场栏目＋留白",
    "声音素材": "绰号候选（绰号—特征—事迹）；签名句式落点段号（全篇总上限）",
    "伏笔总台账": [{{"伏笔编号": "F-01", "内容": "...", "埋设段": "S-001", "回收段": "S-005"}}],
    "炸点总表": [{{"段": "S-003", "层型": "结构·..."}}],
    "情绪地图": "各段轨道一览（戏谑/悲悯/热血＋强度_/5），悲悯段标【悲悯】",
    "布点四件事": "悲悯段落清单｜签名句式落点｜缓冲/商单钩连｜互动钩子",
    "弹药总表": "全篇弹药汇总查重：同词 ≤2 次、同类不连续两段",
    "字数预算表": "各段预计字数合计 = 预算 ±10%",
    "自补清单": "缺失施工件 / 存疑史实及处理方式",
    "封存素材": "素材 → 封存理由（①挂不上主线 ②好看但不推进论点）→ 去向（赘肉/未来篇目）",
    "可信度台账": "F直引{{…}}｜I推断【类型｜依据卡号】{{…}}｜T理论【透镜名｜指向卡号】{{…}}；孤证降I标「存疑」；时间双标点｜数据托底 D卡号｜史源明示",
    "接缝": "上篇末句｜本篇末句→下篇开场前提（单篇填「—」）"
  }},
  "段级施工卡": [
    {{ "段号": 1, "施工卡号": "S-001",
      "五段归属": "开场/翻案/证据群/反转/收束",
      "模块标签": ["钩子", "插播", "缓冲-商单", "定场", "透镜", "视角切换", "互动钩子"],
      "段意": "写出该段的样例；写不出＝这段不该存在",
      "功能类型": "钩子|锚定|主线节点|插件|高潮|收束|衔接",
      "时间码": "00:00–01:18",
      "使用史料卡": ["C-001"],
      "节点分级": "强化|关键|过渡|—",
      "指定技法": ["心理代入", "词汇降维", "场景重构", "反讽", "设问", "史料引用"],
      "因果接口": "本段之因 ← S-___ ｜ 本段之果 → S-___（喂给它的态势/问题/情绪）",
      "弹药清单": "弹药名（类型）｜作用｜推荐度（强烈推荐≤4处/可选）｜原因；无则 空",
      "史料清单": [{{"典籍": "...", "原文摘录": "...", "对应事件卡": "C-001", "来源等级": "A|B|C|D", "用途": "..."}}],
      "歇后语清单": "歇后语＋挂接事件＋建议入点＋（传统/自创）；无则 空",
      "相似事件": "案例＋相似点（须结构同构）＋引申方式（铺垫/对照/判词）；无则 空",
      "炸点": "语言|结构|情绪 类型＋位置 或 无",
      "伏笔": "埋设 F-__ ｜ 回收 F-__ ｜ 无",
      "情绪坐标": "戏谑|悲悯|热血（强度 _/5）；悲悯必须显式写出",
      "预计字数": 350,
      "备注": "只写施工指令，禁写正文句子",
      "五段附加字段": "按五段归属只填对应一组——开场卡：选点/定格/开场之问；证据群卡：分论点/阵型/认知移动/最强反证＋处理/定音句；反转卡：选项集/透镜/因果跨度四字段/价值兜底落点；收束卡：形态/定格单位/甩钩/留白" }}
  ]
}}
"""
    user += rework_section(feedback, prev)
    return system, user, loaded


# ---------------------------------------------------------------- N4 旁白写作

def build_n4_chapter_construction(state, chapter_segments: list[dict],
                                  prev_chapter_tail: str | None,
                                  feedback=None, prev=None,
                                  mounted_skills=None) -> tuple[str, str, list[str]]:
    """逐章施工 prompt（v2 对齐 narration-writer SKILL 第七章：阶段零→阶段一→阶段二）。
v2 修订：引擎②补「抽走测验＋弃弹登记」，弹药权限收紧为只许弃换、不许造弹（缺口走施工异议）；
引擎后新增「每段灵气自检」（黑名单/准确律/情绪目标），对应 SKILL 第五、二章。"""
    system, loaded = _system(
        "n4_narration_construction",
        mounted_skills or DEFAULT_SKILL_MOUNTS["n4_narration_construction"],
        memory_uses=["lessons", "voice_samples"],
    )
    used_cards = [c for c in state.get("event_cards", []) if c.get("本集用不用") != "不用"]
    # N3 新版产出为 {篇级总卡, 段级施工卡}；兼容旧格式（纯段级施工卡列表）
    outline_pack = state.get("outline", {})
    if isinstance(outline_pack, dict):
        master_card = outline_pack.get("篇级总卡", {})
        segment_cards = outline_pack.get("段级施工卡", [])
    else:
        master_card, segment_cards = {}, outline_pack
    voice_benchmark = {k: master_card.get(k, "") for k in
                       ("语气基调", "声音素材", "开头前置", "结尾呼应",
                        "弹药总表", "情绪地图", "可信度台账", "布点四件事")}
    user = f"""## 输入：全篇大纲包（N3 已过闸、N2 弹药挂载后交付：篇级总卡 + 全部段级施工卡——把握全局用，写作范围以本章施工卡为准）
{_j(outline_pack)}

## 输入：本章施工卡（本次只写这几段，逐卡施工）
{_j(chapter_segments)}

## 输入：资料卡包（段内事实唯一来源——施工卡引了哪张卡，本段就只能用哪张卡里的事实）
{_j(used_cards)}

## 输入：篇级声口与风格基准（取自篇级总卡：语气基调 / 声音素材（绰号—特征—事迹、签名句式落点）/ 开头前置 / 结尾呼应 / 弹药总表 / 情绪地图 / 可信度台账）
{_j(voice_benchmark)}

## 输入：前一章末段（保持连贯，接口要焊住）
{prev_chapter_tail or "（本章是全篇开头，无前一章）"}

## 任务：按主技能（narration-writer）第七章「阶段零→阶段一→阶段二」逐段施工
- **本章含第 1 段时**：先过**阶段零**（读料取证——不确定的年份/人名/官职先联网核实，取证在动笔前；按篇级总卡「驱动分型＋结构选型」定调；对施工卡弹药清单逐条斟酌，与史实或声口不合的预先标记）与**阶段一**（声音卡以篇级总卡「声音素材＋语气基调」定稿：绰号—特征—事迹对照沿用 N2 挂载口径、签名句式只服务节奏不承担事实；朝代称呼随语境自然换用——禁用全篇统一的"我X"领属词自称；钩子卡按开场卡附加字段与篇级总卡「开头前置」施工，开场之问必须是"为什么"句式、最后一句落在正题入口）；
- **后续章节**：沿用既有声音卡，与前一章末段声口对齐，不另起炉灶、不中途改称呼策略；
- **每段过四道引擎**（顺序执行）：①心理OS与场景脑补（只填史书留白，不改史实骨架，必挂声明牌"或许/可以想象/估计"）→ ②弹药投放（照单兑现、**不另造新弹**；每发弹药过**抽走测验**——抽走弹药后句子须残废，残废说明是贴的，改写形态焊进句子；宁弃勿堆，弃弹与撤换逐条写进「技法弹药调整声明」；弹药缺口写进「施工异议」退回 N2，不在写作层硬造）→ ③反讽结构（材料有真实"自信→翻车"弧线才用：搭靶用当事人信息集铺到最足，亮锤最短、不加评论，锤长≤靶长1/5；搭靶段禁歇后语泄压）→ ④因果链校验三问（因从哪来／果落在哪／与下段怎么咬合——照施工卡「因果接口」焊死，三问答不出回炉）；
- **每段完稿后过灵气自检**（对应主技能第五、二章）：黑名单逐句扫描命中即改；准确律——这段的灵魂钉死了吗？删掉这个比喻信息会缺损吗？情绪目标（照施工卡「情绪坐标」）兑现了吗、用哪个细节兑现的？答不出＝本段白写，回炉；
- **写的是听的**：一句一个信息、强调词在句尾、大数字先换算；（顿）标停顿位、【上屏】标引文、（地图留白 X 秒）标地图节点；每段正文开头标【段N·功能类型】；
- **悲悯段**（情绪坐标标悲悯者）：零梗零绰号零方言，白描 + 原文直引；进出悲悯轨必补显式过渡句；动情点前后 30 秒不玩梗；
- **史料双保险随写随做**：新时间点年号+公元双标；原文引用配白话翻译（梗只落翻译层、不动原文）；宏大判断后跟数字或原文托底（D 卡托底优先）；**F/I/T 纪律**：施工卡标【I｜推断】的心理动机按声明牌纪律外显（"或许/可以想象/估计"），不把 I 写成 F；【T｜理论透镜】表述只落反转段，不把 T 写成事实断言；
- **零广告**：剥离一切广告植入痕迹。

## 大纲约束纪律
- **硬约束（无权改）**：史实骨架、段序、功能类型、伏笔埋收位置、悲悯段禁区、因果接口——有异议写进「施工异议」，只标注不擅自处理；
- **软约束（可撤换须声明）**：弹药与技法——弹药已由 N2（style-library）按选配五问挂载并过密度红线体检，写作节点正常情况下照单兑现；仅当与上下文声口/史实冲突时保留撤弹/换弹权，逐条写进「技法弹药调整声明」并给出理由；
- 每处埋钩/兑现登记进「本章钩子台账」（埋钩位置 → 计划兑现位置），供缝合节点核销。

## 输出 Schema（只输出 JSON 对象）
{{
  "本章正文": "按成稿格式写的本章 Markdown（【段N·功能类型】标注、（顿）/【上屏】/（地图留白 X 秒）齐全）",
  "技法弹药调整声明": ["段N：撤/弃/换某弹药或技法 ＋ 理由（无则空数组）"],
  "本章钩子台账": [{{"类型": "埋钩|兑现", "内容": "...", "位置": "段N", "计划兑现位置": "段M"}}],
  "施工异议": ["对大纲的疑问，只标注不擅自处理（无则空数组）"]
}}
"""
    user += rework_section(feedback, prev)
    return system, user, loaded


def build_n4_full_script_stitch(state, chapter_drafts: list[str],
                                mounted_skills=None) -> tuple[str, str, list[str]]:
    """全稿缝合 prompt（v2 对齐 SKILL 第七章阶段三）：七任务＝缝合/朗读/双保险/逻辑核销/情绪质检/灵气质检/组装。v2 新增第6任务「灵气质检」（黑名单全文扫描/语频≤3/陌生化≤2/删无感句），自查清单 schema 补对应条目。"""
    system, loaded = _system(
        "n4_full_script_stitch",
        mounted_skills or DEFAULT_SKILL_MOUNTS["n4_narration_construction"],
        memory_uses=[],
    )
    user = f"""## 输入：各章初稿（按顺序，含各章钩子台账）
{chr(10).join(f"--- 第{i+1}章 ---{chr(10)}{d}" for i, d in enumerate(chapter_drafts))}

## 输入：全篇大纲包（N3 产出、N2 挂载后；核对伏笔埋收、呼应归位、悲悯段禁区、因果接口、F/I/T 标注）
{_j(state.get("outline", []))}

## 任务：按主技能（narration-writer）第七章「阶段三」收尾，七任务依次执行
1. **缝合**：逐接口检查——主线按篇型缝合（{"命运感缝合" if state["source_type"] == "person" else "因果缝合"}，用上一段的余震引出下一段的地震），插件进出用挂接句，禁用过渡套话；绰号全篇统一（以篇级总卡「声音素材」为准），朝代称呼随语境自然换用、全篇多样（禁统一"我X"自称）；
2. **朗读测试**：拆读不顺的句子、断一口气读不完的句子；每 60 秒查信息/情绪增量；
3. **史料双保险复核**：每个新时间点年号+公元双标无遗漏；每处原文引用配白话翻译；每个宏大判断有数字/原文托底；反常处证据链 ≥2 条、声明等级到位、孤证只写"存疑"；
4. **逻辑核销**：各章钩子台账逐条核销——埋钩必兑现，兑现不了的删钩改平实表述，不许留坑；设问必已回答；意外类转折已补"能接住意外靠的是……"；决策现场未混入后见之明；收尾回扣开头（开头立的问题/误判，结尾给出答案或推翻，单集逻辑闭环）；
5. **情绪质检**：对照篇级总卡「情绪地图＋布点四件事（悲悯段落清单）」扫悲悯轨（零梗零绰号，改白描+原文直引）、轨道切换过渡句、动情点隔离区；收尾按篇级总卡「结尾呼应」与收束卡施工（收束形态＋定场栏目＋开头设问回收＋留白），末句保留给下篇的钩；
6. **灵气质检**：黑名单全文逐句扫描零命中（不禁让人感叹/可想而知/可歌可泣/历史总是惊人的相似/震惊等）；同一连接词与"就这样"每篇≤3次；陌生化手法全篇≤2处且每处成立；抽走测验抽测弹药句（抽走须残废）；**删无感句**——凡"当时觉得很好、现在无感"的比喻和梗，按准确律重审，不准就删；
7. **成稿组装**：段号标注/【上屏】/（顿）/（地图留白 X 秒）按成稿格式收齐，广告植入痕迹全部剥离。

## 出口硬闸门（必过）
成稿与大纲分别写入文件后运行检查脚本，FAIL 项回到对应段落重写后复检，全绿才可交付：

python3 /skills/narration-writer/scripts/check_output.py <成稿.txt> --outline <大纲.txt>

## 输出 Schema（只输出 JSON 对象）
{{
  "成稿": "# 《...》旁白成稿\\n\\n> 目标时长...\\n\\n## 第一章 ...\\n（完整 Markdown）",
  "自查清单": [
    {{"item": "缝合：接口无过渡套话、绰号统一、称呼多样", "verdict": "过|不过", "location": "..."}},
    {{"item": "朗读：无拗口长句、无一口气读不完的句子", "verdict": "过|不过", "location": "..."}},
    {{"item": "双保险：年号+公元双标无遗漏/原文配译/宏大判断有托底", "verdict": "过|不过", "location": "..."}},
    {{"item": "逻辑：钩子台账全核销、设问全回答、收尾回扣开头", "verdict": "过|不过", "location": "..."}},
    {{"item": "情绪：悲悯轨零梗零绰号、切换有过渡句、隔离区干净", "verdict": "过|不过", "location": "..."}},
    {{"item": "灵气：黑名单零命中/语频≤3/陌生化≤2/抽走测验过/无感句已删", "verdict": "过|不过", "location": "..."}},
    {{"item": "组装：段标/（顿）/【上屏】/地图留白齐全、零广告", "verdict": "过|不过", "location": "..."}}
  ]
}}
"""
    return system, user, loaded


# ---------------------------------------------------------------- N5 成稿审核

def build_n5_three_gate_audit(state, scan_findings: dict,
                              mounted_skills=None) -> tuple[str, str, list[str]]:
    system, loaded = _system(
        "n5_three_gate_audit",
        mounted_skills or DEFAULT_SKILL_MOUNTS["n5_draft_three_gate_audit"],
        memory_uses=[],
    )
    user = f"""## 输入：旁白成稿（被审对象）
{state.get("script_md", "")}

## 输入：单集大纲文件（结构门禁对照基准）
{_j(state.get("outline", []))}

## 输入：事件卡（史实门禁对照基准）
{_j(state.get("event_cards", []))}

## 输入：确定性扫描结果（代码已扫，你复核并补漏；扫描管「词」，你管「句式模式」与判断）
{_j(scan_findings)}

## 任务
过三道门禁，逐项举证（过/不过+具体位置+判罚依据），输出按主技能 Output Schema 的 JSON 对象。
确定性扫描有硬伤的项直接判不过；你发现的扫描漏网问题追加进对应门禁。
最后使用humanizer-zh-next技能把文章润色一遍，去掉AI味。
"""
    return system, user, loaded


# ---------------------------------------------------------------- N6 画本加工

def build_n6_storyboard_translation(state, pronunciation_dict: dict,
                                    feedback=None, prev=None,
                                    mounted_skills=None) -> tuple[str, str, list[str]]:
    system, loaded = _system(
        "n6_storyboard_translation",
        mounted_skills or DEFAULT_SKILL_MOUNTS["n6_storyboard_translation"],
        memory_uses=["lessons"],
    )
    user = f"""## 输入：旁白成稿（审核通过版）
{state.get("script_md", "")}

## 输入：大纲情绪坐标（情感标注的图纸——照抄翻译）
{_j([{"段号": s.get("段号"), "情绪坐标": s.get("情绪坐标"), "功能类型": s.get("功能类型")} for s in state.get("outline", [])])}

## 输入：系列读音词典（累积资产，直接复用；发现新专名请补充）
{_j(pronunciation_dict)}

## 任务：画本加工（弹药的「击发」工序）
1. 拆分单元：按语义拆 1-3 句的合成单元，每单元 ≤80 字；
2. 停顿翻译：（顿）→ 前/后停顿毫秒（炸点后 400-600ms、动情点后 600-800ms、章间 800-1000ms）；
3. 情感标注：抄大纲情绪坐标翻译（高位起跳→明亮有力；回落蓄力→平稳放缓；峰顶→按炸点类型；谷底→低沉克制）；
4. 情感强度克制在 0.6-0.8；情感切换粒度为「段」不为「句」；
5. 读音表：扫多音字/生僻字/专名；数字年代转口语读法；
6. 古文引文单独拆单元，标「换声线感」；（地图留白 X 秒）单元设时长目标。
铁律：只加声音指令，不改事实与措辞——读不顺的句子是写作层放水，写进「打回写作层条目」。

## 输出 Schema（只输出 JSON 对象）
{{
  "画本": [{{"段号": 1, "对应成稿段": "【段1·钩子】", "文本": "...", "拆分": ["...", "..."],
    "情感": "...", "情感强度": 0.7, "语速": 1.0, "前停顿_ms": 0, "后停顿_ms": 500,
    "读音表": {{"妺喜": "mò xǐ"}}, "时长目标_s": null, "备注": "..."}}],
  "新增读音": {{"专名": "拼音"}},
  "打回写作层条目": []
}}
"""
    user += rework_section(feedback, prev)
    return system, user, loaded